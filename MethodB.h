#pragma once

namespace MethodB
{
	bool compress(std::string infile, string outputfile, std::string genomefile);
	bool decompress(std::string inputfile, std::string outputfile, std::string genomefile);
}
